Valentine 
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>For You ❤️</title>

<style>
body {
    height: 100vh;
    margin: 0;
    background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: 'Comic Sans MS', cursive;
    overflow: hidden;
}

.card {
    background: white;
    padding: 40px;
    border-radius: 20px;
    box-shadow: 0 15px 30px rgba(0,0,0,0.25);
    text-align: center;
    width: 340px;
}

h1 {
    color: #ff4d6d;
}

button {
    border: none;
    padding: 12px 25px;
    font-size: 18px;
    border-radius: 30px;
    cursor: pointer;
    transition: all 0.3s ease;
    margin: 10px;
}

#yes {
    background: #ff4d6d;
    color: white;
    opacity: 0.4;
    pointer-events: none;
}

#no {
    background: #ccc;
    position: absolute;
    pointer-events: none;
}

#note {
    font-size: 14px;
    color: #777;
}

#result {
    display: none;
}

#result img {
    width: 220px;
    margin-top: 15px;
    border-radius: 15px;
}

.surprise {
    margin-top: 10px;
    font-size: 16px;
    color: #555;
}
</style>
</head>

<body>

<!-- QUESTION CARD -->
<div class="card" id="question">
    <h1>Will you be my Valentine? 💖</h1>
    <p id="note">You can’t say yes yet… try NO 😌</p>

    <button id="yes" onclick="yesClicked()">Yes 💕</button>
    <button id="no">No 🙄</button>
</div>

<!-- RESULT CARD -->
<div class="card" id="result">
    <h1>I KNEW IT 😏🙃</h1>
    <p>Your choice has always been great ❤️</p>

    <!-- 🔴 REPLACE IMAGE LINK -->
    <img src="YOUR_IMAGE_LINK_HERE" alt="Us">

    <div class="surprise">A surprise is awaiting for you 🤔😉</div>
</div>

<!-- AUDIO -->
<audio id="song">
    <!-- 🔴 REPLACE WITH YOUR MP3 LINK -->
    <source src="BAKHUDA_TUMHI_HO_MP3_LINK" type="audio/mpeg">
</audio>

<script>
let activated = false;

const noBtn = document.getElementById("no");
const yesBtn = document.getElementById("yes");

document.addEventListener("mousemove", (e) => {
    if (activated) {
        noBtn.style.left = e.pageX + 15 + "px";
        noBtn.style.top = e.pageY + 15 + "px";
    }
});

noBtn.addEventListener("mouseover", () => {
    if (!activated) {
        activated = true;

        yesBtn.style.pointerEvents = "auto";
        yesBtn.style.opacity = "1";
        yesBtn.style.fontSize = "24px";

        document.getElementById("note").innerText =
            "Okay okay… now choose wisely 😄";
    }
});

function yesClicked() {
    document.getElementById("question").style.display = "none";
    document.getElementById("result").style.display = "block";

    const music = document.getElementById("song");
    music.play();
}
</script>

</body>
</html>